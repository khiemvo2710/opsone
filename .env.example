# GreenNode platform credentials (deploy / AgentBase CLI)
# Copy to .env in repo root — do NOT commit filled values.

GREENNODE_CLIENT_ID=""
GREENNODE_CLIENT_SECRET=""

# OpsOne API LLM (chat) — configure in ai_gen_src/.env:
# LLM_API_URL=https://maas-llm-aiplatform-hcm.api.vngcloud.vn/v1
# LLM_API_KEY=<GreenNode AIP key>
# LLM_MODEL=minimax/minimax-m2.5
